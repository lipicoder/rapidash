//! Rapidash proto buffer defines

syntax = "proto3";

package rapidash;

message SqlParam

service Query {
  // Executors must poll the scheduler for heartbeat and to receive tasks
  rpc sql (SqlParams) returns (QueryResult) {}
}

